name: Frontend Build & Push

on:
  push:
    branches: [main]
    paths:
      - 'code/frontend/src/**'
  pull_request:
    branches: [main, dev]
    paths:
      - 'code/frontend/src/**'

permissions:
  contents: write      

env:
  DOCKERHUB_USER: suzal777

jobs:
  check-frontend-change:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.filter.outputs.frontend == 'true' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect Frontend Changes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            frontend:
              - 'code/frontend/**'

  check-dev-merge:
    runs-on: ubuntu-latest
    needs: check-frontend-change
    if: |
      github.ref == 'refs/heads/main' &&
      needs.check-frontend-change.outputs.changed == 'true'
    outputs:
      is_dev_merge: ${{ steps.check-merge.outputs.is_dev_merge }}
    steps:
      - name: Check if merge from dev
        id: check-merge
        run: |
          if [ "${{ github.event_name }}" = "push" ] && \
             [ "${{ github.ref }}" = "refs/heads/main" ]; then
             echo "is_dev_merge=true" >> $GITHUB_OUTPUT
          else
            echo "is_dev_merge=false" >> $GITHUB_OUTPUT
          fi

  determine-version:
    runs-on: ubuntu-latest
    needs: [check-dev-merge]
    if: |
      needs.check-dev-merge.outputs.is_dev_merge == 'true'
    outputs:
      semver: ${{ steps.semver.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # CHANGED: Added explicit version validation
      - name: Verify previous tags
        id: verify-tags
        run: |
          if ! git describe --tags --abbrev=0; then
            echo "No existing tags found, starting from v1.0.0"
            echo "version=v1.0.0" >> $GITHUB_OUTPUT
            exit 0
          fi

      # CHANGED: Pinned to exact version v6.0
      - name: Get Semantic Version
        id: semver
        uses: mathieudutour/github-tag-action@v6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          release_branches: "main"
          tag_prefix: "v"

  pr-checks:
    name: PR Research Checks
    runs-on: ubuntu-latest
    needs: check-frontend-change
    if: |
      needs.check-frontend-change.outputs.changed == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache node modules
        uses: actions/cache@v3
        with:
          path: |
            code/frontend/node_modules
            code/frontend/.yarn/cache
          key: ${{ runner.os }}-frontend-${{ hashFiles('code/frontend/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-frontend-

      - name: Install ESLint
        run: npm install -g eslint

      - name: Run ESLint (frontend)
        working-directory: ./code/frontend
        run: |
          yarn install --frozen-lockfile
          yarn lint

  build-and-push-frontend-dev:
    runs-on: [frontend]
    needs: [check-frontend-change, pr-checks]
    if: |
      needs.check-frontend-change.outputs.changed == 'true' &&
      (github.event_name != 'pull_request' || needs.pr-checks.result == 'success') &&
      github.ref != 'refs/heads/main' 
    environment:
      name: ${{ github.ref == 'refs/heads/main' && 'production' || 'development' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Docker login
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build and Push Frontend
        run: |
          SHORT_SHA=$(echo $GITHUB_SHA | cut -c1-7)
          BRANCH_NAME=$(echo $GITHUB_REF_NAME | tr '/' '-')
          
            docker build \
              --build-arg VITE_CLERK_PUBLISHABLE_KEY=${{ secrets.VITE_CLERK_PUBLISHABLE_KEY }} \
              -t $DOCKERHUB_USER/frontend:$BRANCH_NAME-$SHORT_SHA \
              ./code/frontend
            docker push $DOCKERHUB_USER/frontend:$BRANCH_NAME-$SHORT_SHA
            
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker logout
        if: always()
        run: docker logout

  build-and-push-frontend-main:
    runs-on: [frontend]
    needs: [pr-checks, check-dev-merge, determine-version]
    if: |
      github.ref == 'refs/heads/main' &&
      needs.check-dev-merge.outputs.is_dev_merge == 'true'
    environment:
      name: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Docker login
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      # CHANGED: Simplified and secured version handling
      - name: Build and Push Frontend
        env:
          VERSION: ${{ needs.determine-version.outputs.semver }}
        run: |
          # ADDED: Version validation
          if [[ -z "$VERSION" ]]; then
            echo "ERROR: Version not determined!"
            exit 1
          fi

          echo "Building version: $VERSION"
          
          # CHANGED: Removed nested conditionals
          docker build \
            --build-arg VITE_CLERK_PUBLISHABLE_KEY=${{ secrets.VITE_CLERK_PUBLISHABLE_KEY }} \
            -t $DOCKERHUB_USER/frontend:latest \
            -t $DOCKERHUB_USER/frontend:${VERSION#v} \  # REMOVED 'v' prefix
            ./code/frontend
          
          docker push $DOCKERHUB_USER/frontend:latest
          docker push $DOCKERHUB_USER/frontend:${VERSION#v}

          # CHANGED: Added error handling for release creation
          if ! gh release create $VERSION \
            --title "Release $VERSION" \
            --notes "Production release" \
            --target main; then
            echo "Failed to create release"
            exit 1
          fi

      - name: Docker logout
        if: always()
        run: docker logout     